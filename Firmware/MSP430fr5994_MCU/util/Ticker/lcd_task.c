#include "lcd_task.h"
#include <stdio.h>

void mc_lcd_task(){
    printf("I Don't know if I should create a class or not?");
    printf("What do we want to say on LCD Ticker?");
    printf("I Think we need a data class to record whta needs to be written");
};

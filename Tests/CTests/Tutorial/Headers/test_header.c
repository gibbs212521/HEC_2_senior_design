#include <stdio.h>
#include <stdlib.h>
#include ".\Headers\BuckysInfo.h" 
// Quotations mean to search in current directory and subdirectories

int main()
{
    int age = (AGE/2) + 7;
    printf("I shall not confirm nor deny these contents. %d per %s", age, MYNAME);


    return 0;
}